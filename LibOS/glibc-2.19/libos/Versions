liblibos {
  GLIBC_2.12 {
    # checkpoint.c
    checkpoint;

    # sandbox.c
    sandbox_create; sandbox_attach; sandbox_current;

    # advmsg.c
    send_request; extend_request; send_response; send_user_data; read_nonuser_data; gather_response; read_user_data;      

    # msgpersist.c
    msgpersist;

    # benchmark.c
    benchmark_rpc; send_rpc; recv_rpc;
  }
}
